---
title: Docker Cheat Sheet
description: Commonly used docker commands
slug: commonly-used-docker-commands
date: 2023-10-28
type: Post
---

# Docker Cheat Sheet

#### Porcess Management

```shell
# show run docker container
docker ps

# show all docker container
docker ps -a

# run a container
docker run <image>:<tag>

# run a container in thr background
docker run -d <image>:<tag>

# stop a container
docker stop <container>

# kill a container
docker kill <container>

```

#### Volumes & Ports

```shell
# list volumes
docker volume ls

# create a volumes
docker volume create <volume>

# delete a volume
docker volume rm <volume>

# show volume metadata
docker volume inspect <volume>

# delete all volumes not attached to a container
docker volume prune

# mount a local directory to your container
docker run -v <local_dir>:<container_dir> <image>

# copy file or folder from a docker container to host machine
docker cp <container>:<container_dir> <local_dir>

# copy file or folder from local machine onto a container
docker cp <local_dir> <container>:<container_dir>

# map a local port to a docker instance
docker run -d -p 127.0.0.1:<local_port>:<docker_port> <image>

# list the ports a docker container is running on
docker port <container>

```

#### Docker Compose

```shell
# start your docker-compose defined resources in detached mode
docker-compose up -d -f <docker_compose_yml>

# stop all docker-compose resources
docker-compose stop

# destory all docker-compose resources
docker-compose down

# show docker-compose processes
docker-compose ps

# show docker-compose logs
docker-compose logs

# show docker-compose resource consumption
docker-compose top
```

#### Images/Repository

```shell
 # list availabel local imagess
 docker images

 # search for docker images
 docker search <image>

 # pull a docker image
 docker pull <image>

 # build an image with a dockerfile
 docker build -t <image>:<tag> <run_directory> -f <dockerfile>

 # login a remote repository
 docker login <respository>

 # push an image to your remote repository
 docker push <image>:<tag>

 # remove a local docker image
 docker rmi <image>:<tag>

 # show metadata for an image
 docker inspect <image>

 # remove all unused docker images
 docker images prune
```

#### Troubleshooting

```shell
# show the logs of a container
docker logs <container>

# follow/tail the logs of a container
docker logs -f <container>

# show timestamps on docker logs
docker logs -t <container>

# show detail/metadata of a container
docker inspect <container>

# show a 'top' view of processes running on a container
docker top <container>

show a 'top' view of all docker containers
docker stats

# show any files that have changed sinces startup
docker diff <container>

# connect to an already running container
docker attach <container>

# execute a cmmand on a container
docker exec -it <container_id> /bin/bash

# show docker system wide information

# show docker disk space used
docker system df
```

#### Dockerfile

```dockerfile
# take image
From node

#
WORKDIR /app

COPY . /app/

RUN npm install

EXPOSE 80

CMD ["node", "server.js"]
```
